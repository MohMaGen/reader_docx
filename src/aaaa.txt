
    fonts.insert(
        1,
        Rc::new(ttf_context.load_font(font_src, 1 * ML as u16).as_anyhow()?),
    );
    fonts.insert(
        2,
        Rc::new(ttf_context.load_font(font_src, 2 * ML as u16).as_anyhow()?),
    );
    fonts.insert(
        4,
        Rc::new(ttf_context.load_font(font_src, 5 * ML as u16).as_anyhow()?),
    );
    fonts.insert(
        6,
        Rc::new(ttf_context.load_font(font_src, 8 * ML as u16).as_anyhow()?),
    );
    fonts.insert(
        7,
        Rc::new(
            ttf_context
                .load_font(font_src, 10 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        8,
        Rc::new(
            ttf_context
                .load_font(font_src, 11 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        9,
        Rc::new(
            ttf_context
                .load_font(font_src, 12 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        10,
        Rc::new(
            ttf_context
                .load_font(font_src, 13 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        11,
        Rc::new(
            ttf_context
                .load_font(font_src, 14 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        12,
        Rc::new(
            ttf_context
                .load_font(font_src, 16 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        13,
        Rc::new(
            ttf_context
                .load_font(font_src, 18 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        14,
        Rc::new(
            ttf_context
                .load_font(font_src, 19 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        15,
        Rc::new(
            ttf_context
                .load_font(font_src, 20 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        16,
        Rc::new(
            ttf_context
                .load_font(font_src, 21 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        17,
        Rc::new(
            ttf_context
                .load_font(font_src, 22 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        17,
        Rc::new(
            ttf_context
                .load_font(font_src, 23 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        18,
        Rc::new(
            ttf_context
                .load_font(font_src, 24 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        19,
        Rc::new(
            ttf_context
                .load_font(font_src, 25 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        20,
        Rc::new(
            ttf_context
                .load_font(font_src, 26 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        21,
        Rc::new(
            ttf_context
                .load_font(font_src, 27 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        22,
        Rc::new(
            ttf_context
                .load_font(font_src, 29 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        23,
        Rc::new(
            ttf_context
                .load_font(font_src, 30 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        24,
        Rc::new(
            ttf_context
                .load_font(font_src, 32 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        48,
        Rc::new(
            ttf_context
                .load_font(font_src, 64 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        75,
        Rc::new(
            ttf_context
                .load_font(font_src, 100 * ML as u16)
                .as_anyhow()?,
        ),
    );
    fonts.insert(
        150,
        Rc::new(
            ttf_context
                .load_font(font_src, 200 * ML as u16)
                .as_anyhow()?,
        ),
    );

